        if moving_left:
            position_offset[0] += WALKING_SPEED
        if moving_right:
            position_offset[0] -= WALKING_SPEED
        if moving_up:
            position_offset[1] += WALKING_SPEED
        if moving_down:
            position_offset[1] -= WALKING_SPEED